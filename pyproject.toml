[project]
name = "wusaki-agent"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
wusaki-agent = "main:main"

[tool.black]
line-length = 88
target-version = ["py314"]
include = '\.pyi?$'

[dependency-groups]
dev = [
    "rich>=15.0.0",
]
